# 更新日志

### v1.1.0
- **双服资源路由**：选项与答案曲绘随当前题库服务器自动切换：日服使用 `https://storage.exmeaning.com/sekai-jp-assets`，国服使用 `https://storage.exmeaning.com/sekai-sc-assets`；缓存按服务器隔离；新增 `jp_resource_url_base` / `sc_resource_url_base` 配置项

- **题库 master 数据自动同步**：歌名/中文译名/别名改从 Haruki master 数据与社区数据源自动拉取，每 24 小时检查一次，新歌随游戏版本更新自动进入题库（可猜曲目 = 本地歌词库 ∩ 对应服务器曲目）；GitHub API 不可用时回退 jsDelivr，dataVersion 未变化时跳过大文件下载
- **日服/国服题库切换**：新增 `歌词猜曲切换日服题库` / `歌词猜曲切换国服题库` 指令，按群独立记忆，结算显示当前题库服务器
- **QQ 官方机器人 markdown 机制**：官机开局/结算消息以 markdown 渲染，开局附「仅退出本局 / 退出自动模式」连接按钮；结算附切换题库、绑定QQ、个人分数、排行榜连接与快捷入口；新增 `歌词猜曲绑定 QQ号` 账号绑定（分数自动迁移）
- **排行榜改版**：排行榜改为与猜卡面插件一致的横向表格样式（奖牌、未绑定QQ徽章、正确率等）
- **统一退出机制**：`仅退出本局` 只结束当前对局（自动模式继续），`退出自动模式` 停止自动续局（本局继续打完），任何时候可触发；旧的 `退出` 指令废除
- **自动模式优化**：自动模式期间不出现任何 markdown 按钮，结算只显示结果、歌名与下一局提示，退出自动模式后恢复
- 新增 `default_server`、`update_interval_hours`、`connect_link_template` 配置项
- 歌词猜曲分数新增 `歌词猜曲个人分数` 别名

### v1.0.1

- 新增 `max_attempts_per_player` 配置项：每位玩家独立作答次数限制
- 新增 `max_attempts_total` 配置项：全局总作答次数限制
- 新增 `lyrics_display_mode` 配置项：支持图片模式（image）和纯文本模式（text）
- 多人游戏体验增强，作答反馈与结果统计更完善

### v1.0.0

- 实现基本功能
- 歌词猜曲核心玩法
- 用户统计与排行榜系统
- 图片生成与展示
